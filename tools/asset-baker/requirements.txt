# Runtime dependencies for the asset baker. Pinned for reproducible bakes.
# Dev tools (pytest, ruff) live in requirements-dev.txt.

# Numeric / imaging
numpy==2.5.0
scipy==1.18.0
pillow==12.2.0

# Geospatial: DEM sampling, CRS transforms, contour generation
rasterio==1.5.0
pyproj==3.7.2
contourpy==1.3.3

# Tracing
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
opentelemetry-instrumentation-urllib==0.64b0
